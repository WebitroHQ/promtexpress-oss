{
  "name": "promtexpress-oss",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/sdk",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run build && npm run test --workspaces && npm run validate:library",
    "validate:library": "node scripts/validate-library.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.18"
  }
}
